type Callout {
  calloutID: String!
  title: String!
  description: String!
  image: String!
}

type PaginatedCallouts {
  callouts: [Callout!]!
  nextToken: String
}

extend type Query {
  getCallout(calloutID: String!): Callout
  getCallouts(count: Int, nextToken: String): PaginatedCallouts!
}
