type Callout {
  Item: CalloutItem!
}

type CalloutItem {
  calloutID: String!
  title: String!
  description: String!
  calloutImageUrl: String!
}

extend type Query {
  getCallout(CalloutID: ID): Callout
  getAllCallouts: [Callout!]!
}
