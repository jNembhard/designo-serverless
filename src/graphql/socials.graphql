type Social {
  SocialID: String!
  SocialName: String!
  SocialUrl: String!
  SocialIconUrl: String!
}

type PaginatedSocials {
  socials: [Social!]!
  nextToken: String
}

extend type Query {
  getSocial(SocialID: String): Social
  getSocials(count: Int, nextToken: String): PaginatedSocials!
}
