{
  "name": "designo-serverless",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "docker": "docker compose up -d dynamodb",
    "test": "yarn run docker && jest --silent",
    "test:coverage": "yarn run docker && jest spec --coverage --silent",
    "test:verbose": "yarn run docker && jest",
    "test:watch": "yarn run docker && jest test spec --silent --watchAll",
    "test:watch:verbose": "yarn run docker && jest test --watchAll",
    "test:ci": "yarn run docker && jest test spec",
    "deploy": "serverless deploy"
  },
  "author": "Jason Nembhard",
  "license": "UNLICENSED",
  "devDependencies": {
    "@babel/core": "^7.22.10",
    "@babel/preset-env": "^7.22.10",
    "@babel/preset-typescript": "^7.22.5",
    "@types/aws-lambda": "^8.10.102",
    "@types/jest": "^29.5.3",
    "@types/node": "^18.7.13",
    "babel-jest": "^29.6.2",
    "jest": "^29.6.2",
    "prettier": "^2.7.1",
    "serverless": "^3.22.0",
    "serverless-offline": "^12.0.4",
    "serverless-plugin-typescript": "^2.1.2",
    "typescript": "^4.7.4"
  },
  "dependencies": {
    "@aws-appsync/utils": "^1.2.5",
    "@aws-sdk/client-dynamodb": "^3.370.0",
    "@aws-sdk/lib-dynamodb": "^3.370.0",
    "@aws-sdk/util-dynamodb": "^3.370.0",
    "@types/uuid": "^9.0.2",
    "aws-lambda": "^1.0.7",
    "dotenv": "^16.3.1",
    "serverless-appsync-plugin": "^2.3.1",
    "uuid": "^9.0.0"
  }
}
